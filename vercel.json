{
    "builds": [
      { "src": "/app.js", "use": "@vercel/node" }
    ],
    "rewrites": [{ "source": "/api/(.*)", "destination": "/api" }],
    "routes": [
      {
        "src": "/(.*)",
        "dest": "/"
      }
    ]
  }
  